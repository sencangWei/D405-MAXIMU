inherit: "/home/robot/ego_vio_humble/config/mast3r_slam_d405_offline.yaml"

# Offline reverse pass for bidirectional trajectory consistency. The same UMI
# frames and inverted 400 Hz IMU increments are consumed in reverse order.
dataset:
  reverse_order: True
