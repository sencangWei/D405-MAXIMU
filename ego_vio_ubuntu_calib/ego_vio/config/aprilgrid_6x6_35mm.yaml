# AprilGrid 6x6 标定板配置
# 对应: recordings/aprilgrid_35mm.png
# tag 边长 3.52cm, 间距/tagSize=0.3
# 打印后务必用尺子量一个 tag 方块的实际边长,
# 如果跟 35.2mm 不一致, 修改下面的 tagSize。

target_type: 'aprilgrid'
tagCols: 6
tagRows: 6
tagSize: 0.0352       # 单个 AprilTag 方块边长, 单位米
tagSpacing: 0.3       # 间距 / tagSize 的比值
