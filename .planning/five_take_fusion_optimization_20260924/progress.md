# Progress

## 2026-09-24
- Read handoff, repository AGENTS, and debugging/planning/verification guidance.
- Began five-take stage audit; identified take1/take3 first known failure at dense stereo scale.
- Audited all five VINS and stereo stage statuses: all VINS PASS; take1/take3 bidirectional and long-hops PASS with close scales, dense short-hop dispersion alone stops the chain before multisecond.
- Binned accepted dense-stereo observations by 5 s and hop: take1/take3 each show a sustained late scale doubling. Updated diagnosis from “optional dense sensor failure” to “likely time-varying MASt3R visual-scale failure”; no threshold bypass authorized.
- Confirmed the same late ratio in independent stereo report types and MASt3R online/final poses. Read local graph-scale support; next run a diagnostic backend ablation with only already-PASS stereo reports before deciding whether a code change can recover those sessions.
- Ran isolated stage6/7/8 take1/take3 diagnostic using bidirectional+long-hop PASS reports; both graph stages numerically PASS but independent input-quality REJECTS at 6–9 mm stereo edge RMSE and 84–180 mm branch disagreement. No production bypass adopted.
- Inspected representative IR frames around the transition and recorded the qualitative low-texture/planar-scene hypothesis without promoting it to a proven cause.
- Verified a potentially new, not-yet-closed upstream route: MASt3R toolchain has a D405 metric pointmap-scale prior, disabled in production. Sampled stereo depth availability across all five; launched an isolated take1 front-end replay with only this switch enabled, preserving production outputs and without any GT input.
- Independently profiled the other failures: take2 over-10 mm region is sustained at the end, not a few-frame peak; take4/5 rotation metrics depend strongly on position-only SE(3) alignment of near-1D trajectories. Kept the acceptance definition unchanged pending causal validation.
- Completed take1 front-end single-switch A/B: stereo pointmap prior gives almost exact Sim(3) rescaling (0.062 mm residual) with no late-scale improvement. Did not propagate it to five-take production. Prepared one dependent follow-up test that additionally fixes the Sim3 pose scale to unit, so stereo has a chance to constrain per-frame translation.
- Fixed-pose-scale take1 A/B completed and stereo-summary PASS, but direct full-rate check finds 59.7 mm one-frame jump against 3.9 mm VINS at the same instant. Rejected the candidate despite summary PASS. Prepared one final existing-mechanism trial with metric depth anchors plus guarded keyframe PnP; if it cannot remove jumps, leave product unchanged and report that upstream visual/scale work needs a new implementation or better data.
- Metric-depth-anchor/PnP full replay failed with NaNs from frame 832 and late relocalization failure. Tested a non-shipping local window scale prototype across all five: take1/3 improve markedly against VINS but remain too inaccurate, take2 degrades. Stopped this parameter-combination branch.
- Added `trajectory_step_continuity` quality gate in the stereo scale stage, with isolated-jump and sustained-fast-motion tests. Targeted suite 27/27 PASS; real five baseline trajectories PASS and pathological front-end candidate FAIL. Product front-end config remains unchanged.
- Committed the conservative safety gate as `c1abc69b` and pushed only the two owned files to `sencang` branch `dl-fusion/mast3r-d405-深度学习融合` (never `origin`). Fetched the remote commit, restored those files into an isolated `/tmp/umi-remote-verify-*` directory, and confirmed byte-for-byte identity / matching SHA-256. This is a reliability fix, not an ATE improvement.

## 2026-09-25
- Read required handoff and robotics/planning/debugging skill references. Kept the product settings frozen and prototyped an opt-in one-second metric-vector graph factor with synthetic tests.
- Diagnosed a near-linear position-only SE(3) gauge problem: take1/2 position-versus-attitude frame rotations differ by 83.82/20.06°, making initially proposed stereo/VIO agreement testing invalid in the wrong frame. An experimental onboard-attitude alignment restored low take1 stereo/VIO vector residuals.
- Replayed the corrected window factor across **all five** fresh takes. Internal stereo residuals worsened on 1/2/3 and correction clipping appeared on formerly clean 4/5. Replayed attitude alignment alone across all five; that too clipped formerly clean 4/5. Rejected both candidates before Lighthouse scoring. The original production code is to be restored; evidence stays under `metric_window_v1/` and `attitude_alignment_v1/`.
- Restored both tracked source/test files via `apply_patch`; tracked worktree clean and 57 relevant tests PASS. Ran one isolated upstream `tracking.sigma_depth=0.05` front-end replay on take1; 1199 frames complete but the independent D405 dense scale report worsened slightly, so the experiment was rejected before expanding to other takes.
- Instrumented the separate MASt3R fork with a diagnostic online Sim(3) scale CSV and replayed baseline takes1/3/4. Full and online trajectory CSVs are byte-identical to original baselines for all three. The good take4 also has large internal scale variation without metric trajectory warp, falsifying the simplistic “clamp Sim(3) scale” remedy. Production estimator and fusion remain unchanged.
- Committed this diagnostic-only MASt3R fork change as `a2fadd8` on `d405-fork-20260919`, pushed to `sencang-fork`, fetched the remote commit into `FETCH_HEAD`, restored `mast3r_slam/evaluate.py` from that remote commit into an isolated `/tmp/mast3r-remote-verify.*` directory, and confirmed exact SHA-256/content equality. Left the pre-existing `config/base.yaml` whitespace edit untouched.
- Added an opt-in per-frame frontend diagnostic (no estimator input) and replayed full takes1/3/4 with D405 depth sampled every third frame. Both online and final trajectories exactly match the frozen baseline in all three. Same-keyframe stereo/visual motion ratios double in the two failed takes but remain stable in the control; pointmap depth factors and match counts alone are not discriminative. No precision improvement was claimed or promoted.
- Committed the diagnostic to the MASt3R fork as `2956544` and pushed to `sencang-fork/d405-fork-20260919` (not upstream `origin`). Fetched the remote commit and restored `main.py` and `mast3r_slam/tracker.py` into an isolated `/tmp/mast3r-frontend-verify.*` tree; both file hashes and `cmp` match local source. The fork's pre-existing `config/base.yaml` edit remains untouched. Targeted tracker test PASS; a mistaken attempt to invoke a nonexistent `tests/test_stereo_depth.py` yielded no tests, then the actual existing test was rerun successfully.
- 2026-09-25: Cross-five same-keyframe D405 PnP check: failing take1 accepted local ratio 0.277 (frames 750-900) to 0.555 (1050-1200), take3 0.320 to 0.565; good take4 0.271 to 0.266. Median local direction cosine remains 0.994-0.998 and PnP rotation median 0.14-0.28 degrees in these windows. This supports a metric relative-displacement magnitude failure, not a direction flip or fixed extrinsic shift. An opt-in soft relative Sim(3) scale residual, with metric D405 pointmaps enabled, is under take1 replay; it is not a production change or a claimed fix. Static check and one targeted tracker test pass.
- The opt-in soft Sim(3) scale residual completed on take1 and was rejected: accepted D405/visual local ratio in frames 750-900 vs 1050-1200 changed from baseline 0.277/0.566 to candidate 1.085/2.206. Candidate maximum one-frame step was 8.01 mm (no >20 mm jump), but the 2x local scale drift persisted; it mostly changed global gauge. The temporary tracker code and candidate config were removed; candidate output remains isolated for audit. Do not expand this hypothesis to the other four takes.
- 2026-09-25 continuation: Added an opt-in camera-local VINS metric translation residual to the calibrated MASt3R frame-to-keyframe optimizer. Its Sim(3) left-retraction Jacobian passed finite differences (2 new tests, plus 1 existing tracker test). A separate deterministic generator maps frozen Docker2 body poses through the formal `body_T_cam0` to left-IR camera priors with strict timestamp overlap; take1 has 1143/1199 valid camera frames (the first ~1.86s remain unprioritized). On 119 accepted same-hop D405 stereo pairs, local VINS/stereo displacement-vector residual median/P95 is 0.776/1.783mm. A full take1 frontend replay is in progress; this is still an experimental, default-off path and not a claim of improved precision.
- The first local-VINS replay completed but did not improve take1's local scale: stereo/visual ratios 1.086 (frames 750-900) to 2.214 (1050-1200), while max frame step remained 7.91mm. Comparing with the earlier pointmap-scale candidate shows only 0.11-0.41mm median path change by interval: the new factor is largely suppressed. During review we found the first formulation compared meter targets to the *unscaled* keyframe-local Sim(3) translation even though keyframe world scale falls well below one. Corrected the residual and Jacobian to use `world_scale * local_translation`; a new finite-difference regression test passes. Full corrected take1 replay is in progress. Do not judge the local metric-factor hypothesis from the dimensionally incorrect v1 result.
- Operational note: the replay command used `REUSE_DATASET_DIR`, but the workflow actually reads `MAST3R_REUSE_DATASET_DIR`; it therefore rebuilt the dataset harmlessly. Future candidate replays must use the correct environment name to avoid an unnecessary ~30s prepare stage.
- The dimensionally corrected local metric residual (v2) completed on take1; accepted stereo/visual ratios were 1.090 (750-900) and 2.215 (1050-1200), max frame step 8.02mm. Compared to v1, it moved the path at most 1.65mm, so unit repair alone still does not overcome the visual loss. A synthetic normal-equation test now proves that normalization of correlated visual rows *after* existing robust weighting changes the prior/visual balance as intended, without affecting the disabled path. The opt-in v3 take1 replay uses this single additional change. Dataset reuse is confirmed by the workflow log with the correct env name.
- v3 take1 completed but still fails the local-scale check: accepted stereo/visual ratios 1.093 (750-900) → 2.248 (1050-1200), although max frame step is 6.47mm. Root-cause audit of keyframe indices found that take1's keyframe remains frame 0 until 27.73s/frame 832; frozen VINS has no pose until frame ~56/1.86s. The v1-v3 factor demanded a VINS pose at *both* current frame and keyframe, so it was inactive throughout the main bad interval. Added a no-extrapolation anchor at the first VINS-valid tracked visual pose to bridge only this pre-initialization keyframe, with a synthetic rotation/translation test. Six targeted fork tests pass. Full v4 take1 replay is in progress.
- v4 take1 completed. The **online** local D405/visual motion ratios in frames 750–900 / 900–1050 / 1050–1200 are 0.992 / 0.999 / 0.965, demonstrating that the onboard metric tracker factor works. The **final** trajectory regresses to 1.077 / 1.175 / 2.229. Final reanchoring in `evaluate.save_full_traj` multiplies every stored frame-relative Sim(3) pose by the CUDA-global-optimized keyframe pose; global optimization has no corresponding VINS metric factor. Within final keyframe intervals 34.93–38.37 s, final motion is only 0.414–0.468× the online motion. The online trajectory itself has four 26–63 mm keyframe-transition jumps, so neither raw online nor raw final is shippable. Next: constrain metric keyframe scale/translation across the global backend, then test all five independent takes before any product promotion.
- A default-off hard VINS keyframe center/scale anchor after visual graph optimization was exercised on take1 and **rejected**. The calibrated backend printed its activation, but final keyframe quaternion became nonfinite from the first new keyframe at 27.73s; `dataset_full.txt` has only the 832 pre-keyframe rows finite and trajectory conversion fails. The online pose stream remains finite, so this is a backend consistency failure, not missing VINS input. Do not repeat this projection on other takes or ship it. The implementation is experimental and must be removed/replaced by metric residuals inside the optimizer equations. An initial misleading startup config print showed `use_calib: false`, but `--calib` overrides it to true before backend starts; the active path is calibrated GN.
- Removed the failed hard-anchor code and verified the calibrated backend source returned to its original diff. Committed the independently useful default-off local VINS camera-motion factor and regression tests as `32988ef` to `sencang-fork/d405-fork-20260919`; fetched the remote and restored both files into `/tmp/mast3r-vins-verify.wZXeva`, with exact SHA-256 match. Committed the strict timestamp/`body_T_cam0` camera-prior generator plus test as `6f2b9e8e` to `sencang/dl-fusion/mast3r-d405-深度学习融合`; fetched and restored both files into `/tmp/d405-priors-verify.I9ai3V`, again exact SHA-256 match. Pre-existing fork `config/base.yaml` whitespace edit untouched; no `reports/` staging.
- Implemented a new default-off metric keyframe factor **inside** the calibrated CUDA graph normal equations, preserving the original visual-only interface. CUDA extension rebuilt successfully once `cudafe++` was included in PATH; both old/new symbols load and 8 related tests pass. Isolated take1 v6 replay completes 1199/1199 finite frames with no >20mm steps; final max step 10.76mm (v4 online had four 26–63mm jumps). Fixed accepted D405 PnP pairs show late stereo/visual median ratios 0.975 / 0.972 / 0.955 across frames 750–900 / 900–1050 / 1050–1200 (v4 final 1.089 / 1.156 / 2.270). Camera-local vector residual P95 drops from v4 final 7.01 / 20.51 / 24.83mm to v6 3.17 / 5.92 / 8.51mm. A freshly computed dense stereo report, not merely reused baseline pairs, is now running. This is **not** yet an ATE or all-five pass claim; candidate source is uncommitted pending cross-take verification.
- Fresh take1 candidate reports all PASS: dense (376 obs, scale 0.9689), bidirectional (341, 0.9659), long-hop (98, 0.9729), multisecond (45, 0.9957), plus IMU, graph, and input quality. The complete current fusion chain produces position RMSE/P95/max 2.92/7.48/9.23mm and 1143/1143 within 10mm, but rotation RMSE 2.79° exceeds the 2° gate: overall **FAIL**. Fused trajectory differs from VINS-only by 4.71mm SE3-aligned position RMSE, so it is not a bytewise/copy fallback to VINS. Lighthouse was used only after candidate freeze for scoring, not selection or estimator factors.
- Same frozen v6 frontend completed takes3/4. Naively computed output-row max steps 144.6/25.3mm were **not** single-frame jumps: take3 skipped 22 tracked poses over 0.733s (concurrent VINS motion 147.6mm), take4 skipped 3 poses over 0.100s (VINS 26.4mm). This is temporary visual relocalization with full raw camera frames, requiring gap-aware continuity/interpolation, not per-row spike removal. On fixed accepted D405 PnP pairs, take3 late stereo/visual ratios 1.10/1.02/1.31 (frames 750–900/900–1050/1050–1200), while take4 1.02/0.95/1.02. Fresh dense stereo reports for both are running; takes2/5 frontend started unchanged. The take3 log also shows a sustained `metric_scale_jump_rejected` block with fallback pointmap scale ~0.075 while candidate grows to 0.18–0.42; do not change the 25% guard until report/trajectory causality is checked.
- All five frozen v6 frontends have now completed. Independent external front-end position max errors for takes1–5 are 14.23/22.93/22.19/16.67/32.16 mm; these are **not final fusion scores** and the candidate is not cross-take successful. Take3's fresh D405 dense scale has 745 accepted observations and low 0.177 relative dispersion but falsely FAILED the older continuity gate: it treated 0.733s/22 missing visual frames as a 143mm single-frame jump. Take4 dense PASS (675 accepted, dispersion 0.157). Patched continuity to use recorded timestamps, exclude temporal gaps from one-frame jump tests, and report `unverified_gap_count`/`max_gap_s` explicitly. Five focused tests pass; applying the new gate to the already-scored take3 trajectory yields PASS, one reported 0.733s gap, max *contiguous* step 19.59mm. Fresh take2/3/5 dense reports and take4 bidirectional report are running. This patch does not repair or conceal take3's remaining 22.19mm raw ATE max.
- Fresh dense stereo reports for takes2/3/5 all PASS, with scales 0.963/0.987/0.960 and relative dispersions 0.196/0.177/0.262. Take3's regenerated report explicitly shows one unverified 0.733s gap and zero contiguous jump violations. Take4 bidirectional and multisecond also PASS (scale 0.965/0.986). The candidate remains unapproved because independent front-end ATE and take1 rotation gate fail. VINS-only take5 external max was 24.34mm, versus v6 frontend 32.16mm and prior selected fusion 8.56mm; a globally strong VINS metric factor can harm a sequence where baseline stereo already passed. No GT-based switch has been added.
- Backed up the **default-off experiment**, not a product promotion: fork commit `a3a893d` to `sencang-fork/d405-fork-20260919`, fetched and restored six exact files under `/tmp/mast3r-v6-verify-20260925`, all `cmp` identical. Eight fork tests PASS. Backed up the timestamp-aware continuity repair as main commit `4398521b` to `sencang/dl-fusion/mast3r-d405-深度学习融合`, fetched and restored its two exact files under `/tmp/d405-continuity-verify-20260925`, both `cmp` identical; 29 relevant tests PASS. Existing fork `config/base.yaml` remains unstaged; no `reports/` staging.
- Take4 v6 complete internal reports PASS: D405 stereo bidirectional/long/dense/multisecond scales 0.965/0.961/0.968/0.986; IMU scale 0.989; graph and independent input quality PASS. Complete fused GT score is position RMSE/P95/max 4.95/10.05/12.87mm, rotation RMSE 2.81°, **FAIL**, and is worse than prior selected take4 max 6.07mm. This confirms the new always-on VINS keyframe prior is not a universal improvement.
- The 3-frame visual gap in take4 caused two more **pipeline**, not estimator, failures: one saved keyframe has no pose within the old 20ms tolerance (nearest is 39.25ms away inside the 100ms missing-pose interval); the original Gaussian smoother rejects all nonuniform timestamp sequences. Repaired keyframe mapping to skip only timestamps provably inside a visual-pose gap and report their count/error; normal out-of-range timestamps still fail. Repaired Gaussian smoother to work on each contiguous 30Hz segment without blending across gaps and report `temporal_gap_count`. No frames were fabricated or removed by these patches. All 92 related tests pass; take4 graph/quality/smoothing now run and score. Committed main `7d35a196`, pushed only four owned files to `sencang`, fetched and restored those files under `/tmp/d405-missing-poses-verify-20260925`, all `cmp` identical. Take3 graph and remaining take2/take5 stereo reports are running.
- Take3 complete v6 fused GT score: RMSE/P95/max 5.25/7.99/11.87mm, rotation 3.73°, 18 samples over 10mm (one isolated near 20.40s, 17 contiguous at 25.30–25.83s). The same interval's D405 short-window stereo vector residual P95 is only 2.90mm for the fusion (VINS 2.94mm), so the 11.87mm absolute discrepancy is not directly explained by a large 0.1s local stereo mismatch. VINS-only take3 also peaks in 25–26s (13.5mm), which is shared evidence but not proof of GT error.
- **Branch-identity correction:** An exploratory comparison initially used `take2/fusion/sparse/trajectory_fused.csv`, an **unselected** intermediate, giving 35–38s D405 vector residual P95 7.24mm. The actual prior selected candidate is `.planning/imu_scale_orientation_20260924/selected/take2/trajectory_fused.csv`; its same-window residual P95 is **3.37mm** (VINS 2.29mm). Retracted the claim that the *selected* take2 tail has a 7.24mm short-edge residual. True selected GT reports in that directory confirm take2/4/5 max 14.29/6.07/8.56mm. Any further root-cause work must distinguish selected candidates from sparse intermediates.
- Take2 and take5 v6 graph attempts stopped at the unchanged 5% cross-window stereo-scale consistency gate: take2 short ~0.96 vs multisecond 1.056; take5 short ~0.96 vs multisecond 1.026. All individual reports PASS, but long windows have only 89/79 accepted of 619 candidates and sample earlier time spans. Their **original** frontends had cross-window scales 0.326/0.336 and 0.361/0.360, so the cross-window inconsistency was introduced by this v6 frontend, not inherently by those recordings. Do not bypass the gate or promote v6 as universal.
