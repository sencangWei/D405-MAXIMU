# 进度日志

## 2026-08-12

- 建立产品级自动回环与开放轨迹盲测计划。
- 明确人工首尾约束不进入生产链路。
- 下一步：审计 VINS 官方 loop_fusion、本地 ROS2 端口和轨迹/关键帧可用数据。
- 已确认本地端口缺失回环模块，官方模块包含 DBoW2 + 几何验真 + 位姿图优化。
- 正在评估已有 ROS2 Humble 端口，优先复用经移植的官方实现。

## 2026-08-13

- 已移植并接通官方 VINS loop_fusion：DBoW2/BRIEF 候选检索、ORB+双向Hamming匹配、基础矩阵与PnP几何验真、4DoF位姿图优化。
- 已保存可回滚检查点：VINS提交 `334e0cb`，标签 `backup/auto-loop-consistency-20260813`。
- 三帧确认版在 `20260813_000933` 上把首尾3秒中心差从 2.62 cm 降至 1.57 cm，但Z方向存在过修正。
- 五帧一致性中值融合版在同一数据上达到 0.83 cm，XYZ中心差为 0.25 / -0.78 / 0.10 cm；Z跨度保持 27.917 cm，与基线一致。
- 同一数据前30秒非闭环片段：0次自动闭环接受，未观察到误拉回。
- 修复接受/冷却后候选缓存未清空的状态边界，避免旧证据累计触发第二条伪回环；已重新编译。
- 正在用独立平面闭环与升降数据做完整交叉验证；算法从未接收人工终点信息。
- 独立平面闭环 `155408`：前56.74秒获得3/5次确认，安全门槛未接受，轨迹保持基线首尾3秒中心差1.64 cm、Z跨度1.87 cm。
- 独立升降 `151212`：完整1743位姿、Z跨度42.67 cm，0次接受，自动回环没有抹平真实升降；轨迹与基线一致。
- 强闭环 `000933` 最终复跑：1次五帧自动接受，首尾3秒中心差2.62 cm→1.21 cm（改善54.0%），垂直分量1.57 cm→0.38 cm，Z跨度保持27.917 cm。
- 将ORB特征改为关键帧创建时缓存，避免历史关键帧重复提取；60秒升降数据完整回放由约10分钟级降至约2分钟。
- 测试工具修复ROS已shutdown时重复调用`rclpy.shutdown()`的退出异常。
- 更新无外部真值报告并复制至 `桌面/ego_vio_calib_kit/slam_trajectories/`；仍明确禁止把闭环一致性冒充绝对ATE。
- 用户授权按产品交付思路持续自主优化；目标为无人工提示的真实回环尽可能稳定小于1 cm，同时不得泄露终点或强制压平。
- 重新审计计划与源码能力边界：修正了“左右IR双重验真”和“SE(3)后端”的过度表述；当前真实实现为左IR回环验真 + VINS 3D/PnP + 五帧一致性 + 4DoF位姿图。
- 将产品门槛扩展为数据集隔离、ATE/RPE/漂移率/Z误差/失败率、双IR验真、深度平面门控和失效检测；此前首轮候选不再视为整体交付完成。
- 实现双 IR 回环验真第一版：右 IR 同步订阅、5 ms 时间戳门控、右 IR 双向 ORB 匹配和基础矩阵 RANSAC；只有左 IR PnP 与右 IR 几何同时通过才允许进入多帧回环确认。
- 首次编译因 zsh ROS setup 环境引用不存在的项目 setup.sh 而未启动构建，下一次改用隔离 bash 双 setup。
- 双 IR 回环包 Release 编译通过，loop_fusion 节点读取D405配置并完成8秒启动烟测；仅见项目原有警告。
- 首次强闭环回归未进入SLAM：同一 shell 内 `pkill -f` 匹配自身命令导致退出137；将清场与测试拆开并用安全正则重跑。
- 强闭环 `000933` 双IR回归完成：1743/1743位姿，右IR几何通过14次，最终自主接受1条五帧一致回环；接受附近右IR内点396–653、内点率0.749–0.892。
- 第一次生成指标报告因CLI参数名错误而失败，算法回归结果本身完整保留；按脚本实际参数重跑。
- 第二次报告调用确认 `vio_loop.csv` 与报告器格式不兼容；改用同轮1743样本的 `vio_corrected_stream.csv`，并将统一轨迹格式列入工具修复。
- 强闭环双IR候选指标：首尾3秒中心差2.622 cm -> 1.136 cm，改善56.7%；右IR同步内点验证通过；Z跨度27.917 -> 28.439 cm，仍需后续Z策略验证。
- 升降回归第一轮只输出839/1743位姿，定位为回环可靠大队列对VINS输入形成DDS反压，固定20秒排空又提前结束测试。
- 将回环原始图像订阅改为Best-Effort有限队列，并将离线排空上限参数化为默认180秒；重新编译通过。
- 升降回归第二轮恢复1743/1743位姿，自动回环接受0、几何后拒绝0；完整性和真实升降安全门槛通过。
- 发现直接订阅两路原始IR的回环模块会在Reliable模式反压VINS、在BestEffort模式丢失/错配关键帧；改为VINS在关键帧确定后发布同步双IR旁路。
- 继续发现位姿、点云、双IR分成三个ROS话题仍可在负载下各自丢帧；新增`LoopKeyFrame.msg`，将双IR、位姿和特征点云原子发布，回环端不再依赖到达顺序配对。
- 修复估计器内部关键帧位姿与点云由两个可覆盖单值邮箱独立读取的问题，新增原子`KeyFrameData`。
- 修复测试工具残留回环子进程污染：两个早期`loop_fusion_node`未退出造成`/odometry_rect`同时间戳三份、65m假路径；现启动前拒绝残留节点，停止时等待整个进程组退出。
- 干净强闭环`000933`：1743/1743轨迹，1次五帧自动接受，右IR通过15次、接受附近内点约596；首尾3秒中心差2.622cm→1.136cm，改善56.7%，仍略高于1cm目标。
- 干净真实升降`151212`：1743/1743轨迹，0次自动接受，证明双IR原子回环旁路没有把真实升降误拉平。
- 试验将已接受回环冷却从10帧降到3帧可产生第二条边，但闭合反而退化到1.189cm；已回退到10帧，避免在开发集上追逐偶然指标。
- 自动回环改为DBoW前四候选逐个做双IR/3D-PnP验真，未通过首候选时继续验证后续候选。
- 一致性确认改为连续四帧必须命中同一历史关键帧，修正量用逐轴中值和环形角度中值融合；大于3cm的全局地图修正拒绝执行。
- 产品验收脚本新增C++ 30fps DB3回放、地图跳变、Z跨度保留和无提示闭环首尾1cm门槛；首尾误差仅做后验评分，不输入SLAM。
- `155408`最终复跑：1744/1744位姿，覆盖99.37%，自动接受1条，闭环4.53mm，关键帧与估计器队列零丢失。
- `000933`最终交叉复跑：1742/1742位姿，覆盖99.32%，自动接受1条，闭环6.71mm，Z跨度27.92cm保持100%。
- `151212`最终升降反例：1741/1741位姿，覆盖99.26%，自动接受0条，Z跨度42.67cm保持100%；不再把短时跟踪误当场所回环。
- 测试目录49项全部通过，VINS Release编译通过；全仓pytest会误收集6个硬件诊断脚本，正式软件测试以`pytest tests`为准。
- `121306`开放/近闭环最终复跑：1737/1754有效预期位姿，覆盖99.03%，自动回环0接受，关键帧与估计器队列零丢失；60秒数据墙钟约105秒，离线完整性通过但实时吞吐尚未达标。
- 修正外部真值评测的旋转解释：保留位置Kabsch对齐的3.165°指标，同时新增姿态对齐指标0.319°；两种全局旋转相差3.149°，证明原大值主要来自对齐坐标系歧义而非运动相关旋转漂移。
- 实现离线Depth平面两级质量门原型：局部RANSAC精度/面积门 + 世界重力方向/法向/平面距离聚类门，并用单测证明真实升降不会被“恒高”假设抹掉、稳定墙面不会进入Z约束。
- 修复历史四流会话中0字节占位DB3导致回放打开空库；VINS源提交`ef6b6bc`，Release编译通过。
- 新增不修改原录制的双IR轻量DB3缓存和`--image-db3`回放入口；`001100`缓存回放约实时完成，1339个位姿、覆盖102.6%、回环与估计器队列0丢失，产品验收PASS。
- 建立可执行客户交付门禁：冻结8类动作、每类至少3条隐藏真值运行和ATE/RPE/Z/姿态/失败率阈值；隐藏数据必须同时提供哈希保护的原始真值与评测报告。
- 修正交付状态语义：当前4条已有验证只允许输出`CANDIDATE_PASS`，完整门禁明确输出`NOT_READY`；当前缺直线、L形、原地旋转、快速手持四类动作及完整隐藏外部真值矩阵。
- 将深度平面软约束改为严格因果的过去时序状态机：未来观测不能影响过去输出，修正只沿世界重力轴，限幅3cm、限速3cm/s，失去支持后平滑释放为零；不使用绝对高度、终点或人工闭环答案。
- 新增观测CSV确定性重放入口，避免每次在6GB原始DB3上无索引全表扫描；真实墙面`001100`重放为450帧观测、108帧局部有效、81帧位姿匹配、0个水平候选，因子`DISABLED`且450个轨迹样本最大改变量0m。
- 深度/Z原型针对性9项与全仓65项测试通过；仍缺真实水平面正样本，不进入生产C++主链路、不计为产品功能完成。
