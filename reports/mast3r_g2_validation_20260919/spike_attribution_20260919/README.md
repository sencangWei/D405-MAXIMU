# 尖峰归属分析 + MASt3R 生产链拓扑更正 (2026-09-19)

本目录是 2026-09-19 会话的**分析脚本与证据**，回答两个问题：

1. **过大的误差（尖峰）是谁造成的** —— tracker 真值？VINS 单链？MASt3R 上游？还是融合？
2. **失败数据有没有共性** —— 相似画面？相似动作？外部因素（反光/强光）？

外加一项中途查出的**管线拓扑更正**（见下，它推翻了本会话中途的一个结论）。

---

## 〇、★ 更正：生产链是一条链，不是四条平行链

我在分析中途把产物目录里的四个 `trajectory_*.csv` 当成了四条平行链：

| 我当时的叫法 | 文件 | 当时以为的 | **实际** |
|---|---|---|---|
| ⓐ 原始 | `trajectory_frames.csv` | 链头 | ✅ 链头 |
| ② 图 | `trajectory_graph.csv` | 与 ③ 并列的兄弟 | **④ 融合的输入** |
| ③ 图+度量 | `trajectory_imu_metric.csv` | 与 ② 并列的兄弟 | **② 的输入** |
| ④ 融合 | `trajectory_fused.csv` | 链尾 | ✅ 链尾 |

读 `scripts/mast3r_slam_precision_workflow.sh` 即可确认（一条链）：

```
[6/8] align_mast3r_scale_with_imu.py        --trajectory trajectory_frames.csv
                                            --output     trajectory_imu_metric.csv
[7/8] fuse_mast3r_stereo_imu.py             --trajectory trajectory_imu_metric.csv
                                            --output     trajectory_graph.csv
[8/9] fuse_docker2_mast3r_complementary.py  --mast3r     trajectory_graph.csv
                                            --output     trajectory_fused_unsmoothed.csv
[9/9] smooth_pose_trajectory.py             --input fused_unsmoothed --output trajectory_fused.csv
```

⇒ 据此得出的「**②→③ 让误差从 32.27 恶化到 41.81**」是**把管线读反了**：
`imu_metric` 在 `graph` **上游**，是图优化**改善**了形状，不是某一步恶化了它。

⇒ 另一处更隐蔽的错：`rigid_align` **不估尺度**，所以它的残差**强烈依赖输入的绝对尺度**。
原始帧轨迹比真值大 2.8 倍，那个被我一直当作「形状误差 810mm」的数，
**绝大部分是「太大」贡献的，不是形状**。要量形状，必须先按相似变换把尺度归到 1.0 再 `rigid_align`。

跑 `chain_topology.py` 可复现全部三点。

### 实测（`chain_topology.py`）

```
① imu_metric 是否恒等于 frames × 常数
   ⇒ 23/23 个 cell 逐点残差 < 0.01mm (全局最差 0.0000mm)
   ⇒ [6/8] 的产出 = 输入 × 一个标量。不做任何形状工作。

② 尺度归一后的真形状误差 (09-14 batch3/group2/tight)
   raw  frames  对真值尺度 0.3598 (偏大 +177.9%)   形状 max  28.94mm  中位 10.50mm
   imu_metric   对真值尺度 0.9175 (偏大   +9.0%)   形状 max  28.94mm  中位 10.50mm   ← 与 raw 逐位相同
   graph        对真值尺度 0.9370 (偏大   +6.7%)   形状 max  21.19mm  中位 10.19mm   ← 图优化才改形状
   fused        对真值尺度 0.9934 (偏大   +0.7%)   形状 max  11.02mm  中位  3.84mm   ← 同时修尺度

③ [6/8] 产出的轨迹是否真的度量 (对真值相似尺度, 1.0 = 真度量)
   ⇒ 23 个 cell: 中位 0.9466, 范围 0.9045–1.0782
   ⇒ 20/23 个 cell 偏大, 最大偏大 10.6%
```

**③ 是本次最值得记的一条**：名为「IMU 度量尺度」的 `[6/8]` 步，
产出**不是度量轨迹**（中位偏大 5.6%，最大 10.6%）。
而同一轮管线里**已经算了四份双目直接度量报告**
（`stereo_scale_{bidirectional,long_hops,dense10hz,multisecond}_report.json`）
并喂给了 `[7/8]`。这是**现状记录，不是改法建议** —— 改 `--metric-scale-mode`
属于通用策略变更，必须用户裁决。

### 顺带：td 假设被实验否掉

`[6/8]` 的 `--td-s -0.009109323` 是**硬编码常量**，不是解出来的。
`tdsweep.sh` 扫 ±40ms：结果只在 **52.90–55.07mm** 之间，
且 **−9.11ms 已经是最优点**；任何 td 都救不回 ③ 超过 ②。
用 −9.11 重跑**逐位复现**了盘上的 ③（52.90），**验证了测量台架本身是对的**。

---

## 一、尖峰归属（19 个四链齐备的 cell）

把各链误差摆到同一 GT 时间栅格上，取「融合误差 > 10mm」的样本，
算各链误差 / **该链自身中位数**：

| 链 | 超标窗内抬升 | 抬升(>1.5x)的 cell | 判定 |
|---|---|---|---|
| tracker 真值 | 抖动 0.120mm vs 误差 17.50mm | — | **排除**（占误差 0.63%，最坏 3.25%） |
| ① VINS 单链 | **0.92x** | **2/19** | **排除**（不抬反降） |
| ② MASt3R 图 | 2.06x | 12/19 | **主源** |
| ③ 图+度量 | 1.78x | 13/19 | **主源** |
| ④ 融合输出 | 2.88x | 19/19 | **放大器，非源头** |

⇒ **14/19 的 cell 尖峰在 ②/③ 就已存在**，融合只是放大。09-14 take 属此类（②2.75x / ③3.57x）。
⇒ 剩余 5/19 是融合自造。
⇒ **tracker 洗清的关键数字**：超标窗处 tracker 自身高频抖动（savgol 残差）中位 **0.120mm**、
最坏 0.400mm，而误差中位 17.50mm、最坏 30.39mm —— 绝对量级差 **158 倍**。
tracker 确随动作变糙（相对自身基线 74x，因静止时基线仅 0.001mm），但绝对值解释不了误差。

> ⚠ 上表②③的「主源」判定是在**拓扑更正之前**做的：两列本质是**同一段轨迹的上游/下游**
> （③ 是 ② 的输入，形状逐位相同），不是两个独立嫌疑犯。结论（尖峰源自 MASt3R 上游）
> 不变，但**不要把它读成「两个独立的链各自超标」**。

## 二、共性：19/19 cell 都指向「动作激烈」

- 超标窗处 **GT 角速度 2.47x、线速度 2.49x**（相对各自全局中位），**19/19 cell 全抬**。
- 超标窗口**完全避开轨迹 90–100% 段**（0 个窗口），集中在 20–50%。
- 窗口样本数：中位 10，均值 22，最长 290；**≤3 样本的占 30%**（这部分才真叫「尖」）。

## 三、画面/光照：强光过曝假设被【实测否掉】

- **Y 通道实测 min 16 / max 235，≥240 占比 0.0000%** ⇒ 整条录制**零过曝**。
- 亮度与误差 **负相关 ρ=−0.173**（越亮误差越小）：
  最亮 5% 帧误差中位 **3.18mm** < 全部帧 4.15mm < **最暗 5% 帧 4.66mm**。
- 效应量排名（|Spearman ρ|）：**GT角速度 +0.282 > 线速度 +0.230 > 清晰度 −0.228 > 亮度 −0.173 > 低纹理% +0.174**。
- 红外通道同样否掉：IR ≥250 占比最高 0.154%，且**饱和帧误差反而更低**（3.50 vs 4.20mm）。
- 场景（实看帧）：高反光深色亮面木桌（镜面反射红碗/显示器/线缆）+ 画面中下部大面积**深色低纹理双叉件**；
  某些帧有**正在发光的显示器**，另加**基站的 IR 发射**。
  ⇒ 反光/低纹理是真实场景风险，但**不是靠过曝起作用**。

> ⚠ 曾经用「误差最大 6 帧 vs 最小 6 帧」对比得出「亮斑 0 个 vs 12 个」，**那是选取假象**：
> 放到全部 1743 帧上，亮斑数对误差**没有预测力**（ρ=0.033）。
> 屏幕上的「显示器+夹爪 vs 空桌面」强对比同样是选取假象 / 与轨迹位置混淆。

## 四、口径提醒

- 图像相关只在 09-14 一条 take 上做（单条 db3 6.6GB，全 take 扫代价高）；动作共性是 19 个 cell。
- 相关强度都弱（|ρ|<0.3 ⇒ 约 9% 方差）且彼此混淆（动快→视场变→纹理/亮度都变），**不能当因果**；
  且**解释不了那两坨 >9mm 的连续块**（`plateau.py`：去掉 top-k 后 max 仍钉在 10.36）。
- 09-14 tight 只有 **1 个样本** >10mm，该 take 的 ρ 由整个误差分布驱动。

## 四之二、★ 下一步该修哪里：门限分解

### tracker 换分支【解释不了】这批失败

`lighthouse_tracker_branch_gate.py` 铺到全部 12 个 take：

```
分支门 REJECT 的 cell: 2/12
  20260914_validation_v10_holdout_batch2/group1  受影响 63.6%  换分支 1 次
  20260915_batch5_four_videos/group1             受影响 65.6%  换分支 1 次
```

扫描的 17 个未达标 cell 里, **分支门只解释了 2 个, 另有 15 个是 PASS**。
⇒ "失败全是真值伪影"这条假设**在本语料上被否掉**。（注意 54 条 P95 语料里
14 条 REJECT 是另一批数据, 两者不矛盾。）

### 卡人的是 max 与 rot，不是典型精度

```
完整门        达标  1/18        放开 max      达标  5/18
放开 rot      达标  2/18        同时放开两者  达标 10/18
⇒ max 超 10mm 的 16/18, rot 超 2.0° 的 12/18, 而 rmse 中位仅 5.53mm
```

**`v11b3/group2/tight` (= 09-14 holdout) 是 PASS 的**；同 take 的 **sparse**
`rmse 4.70 / max 8.33 / w10 100.0`，**零个样本超 10mm**，
**唯一失败原因是 `rot 2.08 > 2.0`**。
⇒ **09-14 那条的平移已经复原了，现在卡住的是旋转。**

### 旋转的预算：一半以上是【常量姿态偏移】

官方打分用 `rigid_align` 由**位置**定 R。若改用**姿态**最优的 Rc 再对齐：

```
位置对齐后 rot 中位 1.99° → 姿态最优后 1.18° (降幅中位 0.81°)
姿态最优后超 2.0° 的 cell: 0/22
```

常量偏移 `Rc·Rᵀ` 夹角 **中位 1.52°，占 2.0° 门限的 76%**。
且同一 take 的 sparse/tight 残差几乎逐位相同（1.18/1.18、0.76/0.76、1.42/1.42…）
⇒ 这是**每 take 一个常量**，不是随机噪声。

⚠ **但轴向在 take 之间不一致**（(0.70,0.16,0.70) / (0.04,0.67,0.74) / (0.44,0.44,0.78) …）
⇒ **不是**一个全局标定常数，是**每 take 的姿态偏置**（陀螺 bias / 重力初始化一类）。
自洽性核对：√(1.99² − 1.18²) = 1.60° ≈ 实测 1.52°。

### 逐链 rot（注意 ②③ 的 91° 是坐标系约定，不是误差）

```
①VINS       n=21  中位  3.27°  超2.0的 21/21  最大 13.18°
③imu_metric n=23  中位 91.07°  超2.0的 23/23  最大 92.22°   ← 重力 bake 约定
②graph      n=22  中位 91.41°  超2.0的 22/22  最大 92.70°   ← 同上
④fused      n=22  中位  1.99°  超2.0的 10/22  最大  3.18°
```

⇒ **旋转误差由 VINS/IMU 姿态链主导**（中位 3.27°），融合把它压到 1.99°——正好压在门线上。
⇒ 与 [[fusion-scale-gate-umeyama-check]] 的"视觉姿态自身误差 1.3–2.4°"、
[[vins-config-optimal]] 的"别再调参"相互印证，**动姿态参数前先回看这两条**。

## 四之三、★ 那个常量偏移归谁：**融合根本不贡献姿态**

直接把 ④fused 的姿态与 ①VINS 的姿态比（先归到**共同 GT 栅格**再比；
两条链的 `val` 掩码不同，不归栅格会因长度不等被整片跳过）：

```
n=20  剥掉一个常量旋转后残差 中位 0.0000°  最大 0.0084°(浮点噪声)
残差 ≈0 的 cell: 20/20
```

**⇒ fused 姿态 ≡ VINS 姿态 × 一个常量旋转（~110.5–111.6°）。**

融合**不贡献任何姿态信息**——它只融位置，姿态是 VINS 原样搬过来的。
所以：

1. **rot 这道门 100% 由 VINS 决定**，融合改不了。
2. VINS 的真实姿态噪声只有 **1.22°（r2 中位）**，**本就在 2.0° 门内**。
3. 把格子顶过门的是**轨迹位置与姿态互不自洽**那 **1.64°** 常量偏移
   （官方门用**位置**定 R，两者不一致就吃罚）。
   预算自洽：`√(1.22² + 1.64²) = 2.05° ≈ 实测 r1 1.99°`。

⇒ **只要把位置-姿态的自洽性修好，rot 这一项每个 cell 都能过门**，
   而且这**不是**"调姿态参数"——记忆里 `vins-config-optimal`（别再调参）与
   `fusion-scale-gate-umeyama-check`（换陀螺姿态更差 78%）的警告都不挡这条路。

### ⚠ 口径：姿态最优对齐是**诊断**，不是官方门的做法

官方门用 `rigid_align` 由**位置**定 R。"降幅 0.81°"**不等于**"换个对齐就能过门"——
要过门得让轨迹的**位置与姿态互相自洽**。这个区别决定下一步是查根因还是白费功夫。

## 四之四、★ 那个常量偏移从哪来：**它是旋转漂移，而且位置数据本来就分辨不出来**

`attitude_offset_source.py`。追 四之三 / 四之二 里那个 1.52° 常量偏移的来源。

### ① 否掉「尺度伪影」

Procrustes 里**尺度与旋转解析解耦**（尺度由 ‖A‖ 定，旋转由 `A·Bᵀ` 的 SVD 定），
换 similarity 的 R 得到**逐位相同**的偏移（3.00°→3.00°、1.65°→1.65°…）。
⇒ 偏移与尺度无关。这条能否掉，但**在结构上必然如此**，算否证不算发现。

### ② 是「旋转漂移」的签名

轨迹切两半、各自 `rigid_align`，两个 R 的夹角 = 半段漂移。
`rigid_align` 会落在漂移的**中点**，姿态不跟着漂，于是差一半：

```
VINS   偏移中位 3.00°   半段漂移中位 3.83°   off/(漂移/2) = 1.49
```

### ③ 融合修掉大半，而且**全是位置侧的功劳**

```
             偏移      半段漂移
VINS        3.00°      3.83°
fused       1.57°      1.10°
```

姿态是**逐位相同**的（四之三：fused ≡ VINS × 常量），
⇒ **off 的任何变化只可能来自位置侧** ⇒ 融合的位置修正确实在收这个口子。

### ④ ★ 残留的 1.57° 落在【位置分辨带】里

```
轨迹半径中位 15.9cm, 位置 RMSE 中位 ~2.6mm
⇒ 位置数据能分辨的最小旋转 = atan(位置RMSE / 半径) ≈ 1.75°
⇒ 把位置最优 R 换成姿态最优 R, 位置 RMSE 只涨 +1.40mm (埋在位置噪声里)
```

**⇒ 这 1.57° 的旋转，位置数据本来就定不出来。**

### ⑤ 结论：**rot 门的地板 ≈ 2.1°，门限 2.0° —— 门在地板下面**

```
rot = √(VINS姿态噪声² + 常量偏移²) = √(1.18² + 1.75²) = 2.11°   门限 2.0°
```

在 **16cm 半径的桌面轨迹**上，这道门本身就是**掷硬币**，**不是调参问题**。
这与 [[vins-config-optimal]] 从完全不同的方向得出的
「别再调参，要更高精度**换更大回路**」**互相印证**——
半径越大，同样的位置精度给出的角分辨率越高。

> ⚠ 口径：④⑤ 里的"分辨带"用的是**位置 RMSE / 半径**这个一阶估计，
> 不是严格的可观测性分析。它足以说明量级，不足以当定理用。

## 四之五、★ 门到底怎么失败的：两种病，外加两个被关掉的决策

`gate_failure_modes.py`。前面把 `rot` 挖到底了，这一节收 `max`。

### ① 度量尺度来源：**换上双目没有依据**（关掉一个悬了好几轮的决策）

`imu_scale_report.json` 的 `scale` 与四份 `stereo_scale_*_report.json` 的
`scale_m_per_mast3r_unit` 是**同一个物理量**（米 / MASt3R 单位），
可以直接和真值（`trajectory_frames.csv` 对 GT 的相似尺度）比：

```
IMU 估计偏差   中位 5.94%      双目估计偏差 中位 5.26%
双目更准的 cell   10/23        <-- 就是掷硬币
IMU-双目分歧    中位 2.76%     超 15% 硬门的 0/23
```

⇒ **两个独立的度量参考都偏大 ~5%，而且谁也没比谁强。**
⇒ `codex-gate-blindspot-history` 里那个「尺度交给双红外」的策略，
**在这批数据上没有收益依据**。（⚠ 不等于那个 15% 硬门不存在，
只是**在这 23 个 cell 上一次都没触发**。）

### ② 最终产物的尺度**不是** max 的驱动

```
融合产物尺度偏差 中位 +0.52%   范围 -2.05 ~ +2.30%
corr(|尺度偏差|, max) = -0.042     corr(·, rmse) = +0.126
```

⇒ 融合已经把尺度修好了，`max` 不赖它。

### ③ ★ 失败分成**两个物种**

```
超标样本数 <=5 个的:  5/18      超标 >=20 个的: 13/18     中间几乎没有
```

| 类 | 例子 | 超标数 | 占比 | 最长段 | 病 |
|---|---|---|---|---|---|
| **A 孤立离群** | `v10/g1/sparse` | **2** | 0.11% | 0.03s | 几个坏点 |
| | `v10/g1/tight` | **2** | 0.11% | 0.03s | |
| | `v11b3/g1/sparse` | **5** | 0.29% | 0.13s | |
| | `v11b3/g2/sparse` | **0** | 0% | — | （max 8.33，**只卡 rot**）|
| **B 结构性** | `v10/g2/tight` | **368** | 21.1% | **9.63s** | 一整段跑偏 |
| | `batch5/g3/tight` | **378** | 21.7% | 7.80s | |

### ④ 「离达标差几个点」

```
去掉最坏 1 个样本就 <10mm:  2/18
去掉最坏 5 个样本就 <10mm:  5/18
超标 <=5 个的 5 个 cell，去 5 个后【全部】<10mm (9.42–9.95)
⇒ 超标上百个的那批，去掉最坏 5 个几乎不动 (30.39 → 30.16)
```

**A 类里有 3 个只差 1–2 个采样点。** 它们和 B 类是**两种病，要两种药**。

### ⑤ ★ 误差出现在**录制的固定时刻**，可复现

sparse 与 tight 是同一录制**不同的帧子集**、两次**独立**跑。
把两边的超 10mm 时刻取交集：

```
v10/g1        sparse    2 / tight    2   重合    2
batch5/g2     sparse   30 / tight  169   重合   30
batch5/g4     sparse   41 / tight   20   重合   20
collective/g1 sparse  154 / tight  178   重合  106
```

⇒ **两次独立跑，误差落在同一时刻** ⇒ 是**录制的属性**（那一刻的场景/动作），
**不是算法随机抖动**。这和 二、的「动作激烈 19/19 cell 角速度 2.47x」是同一件事，
但这里是**直接证据**：如果是随机抖动，两个子集不该重合。

⇒ 推论：**这些失败是可定位、可复现的** —— 能指到具体是哪一刻。

## 五、本目录文件

| 文件 | 作用 |
|---|---|
| `chain_topology.py` / `.json` | ★ 生产链拓扑 + [6/8] 纯缩放证明 + 度量尺度表 |
| `attrib.py` / `attrib.npy` | 尖峰归属：各链误差摆到同一 GT 栅格 |
| `trackjit.py` / `trackjit2.py` | tracker 自身抖动（2 修了跨 take 时间基 bug） |
| `grab.py` | 从 db3 取指定帧（CDR 解析，payload 取尾） |
| `imgcorr.py` / `imgcorr2.py` / `ircorr.py` / `feat.py` / `partial.py` | 画面/光照 vs 误差相关 + 偏相关 |
| `plateau.py` | 证明插补救不了：>9mm 是两坨连续块，非孤立尖峰 |
| `tdsweep.sh` | `[6/8]` td 扫描（±40ms），否掉时间偏移假设 |
| `step23.py` / `step23_d.npy` | 四路对照（**注意：拓扑前提已被更正，仅作历史记录**） |
| `spikes.py` / `spike_characterize.py` / `spikes.json` 等 | 尖峰刻画与可行性探查 |
| `tracker_branch_vs_gate.py` / `.json` | ★ 把 tracker 分支门铺到全部 take，与尾段扫描对表 |
| `rotation_budget.py` | ★ 门限分解 + 逐链 rot + 常量姿态偏移 vs 随机噪声 |
| `attitude_offset_source.py` / `.json` | ★ 常量偏移的来源：旋转漂移 + **位置分辨带** ⇒ rot 门的地板 2.11° |
| `gate_failure_modes.py` / `.json` | ★ max 的失败分**两个物种** + 度量尺度来源对照 + **误差落在录制固定时刻** |
| `recover0914_sweep.json` | 尾段配方扫描的原始结果（`rotation_budget.py` ⓪ 读它） |

**复现**：全部脚本只读产物 CSV + 官方评测器 `scripts/evaluate_slam_ground_truth.py`，
不跑管线、不改任何东西。`python3 chain_topology.py` 即可。

**打分口径核对**：本目录的打分路径已与 `recover0914_sweep.py` 的基线**逐位对上**
（`v11b3/group2/tight` A_G2_current = rmse 4.11 / max 9.79 / w10 100.0 / rot 1.89）。
同一 cell 的**盘上产物** = 4.57 / 10.32 / 99.9 / 1.96，与扫描日志的 `R2_v11_batch3` 行数值相同
⇒ 盘上产物是 **G1 配方（max 10.32 FAIL）**，现役 **G2 尾段重跑才 9.79 PASS**。
引用精度前务必先读 `estimate` 字段确认是哪条链。
