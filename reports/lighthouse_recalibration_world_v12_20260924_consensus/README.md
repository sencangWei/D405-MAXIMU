# Lighthouse 世界标定 v12（2026-09-24）

状态：`ACCEPTED_FOR_NEW_EXTERNAL_VALIDATION`。基站在采集与验收期间保持不动；移动的是 Tracker/UMI。旧 v11 配置保留，仅供历史数据复现，不应用于新采集。

## 冻结配置

- 文件：`libsurvive_config_frozen_v12.json`
- SHA-256：`63bd7d886bf6f8b46735a5f38047af8902d1f79b60a6feb57b3e3233e5bc6e99`
- 基站 ID：`2640831677`、`1850292303`
- 两基站间距：`1763.438 mm`（v11 为 `1748.207 mm`）。两解的相对姿态差约 `25.208°`；这说明几何解不同，**不能单独证明基站何时移动**。

## 求解与验收证据

- 原始记录：`../lighthouse_recalibration_world_v12_20260924/run_20260924_171326/lighthouse_raw.rec.gz`；SHA-256：`98f8e1d4ac1eb1de903062fa72153d60d6f764f28ec76678562999e83604948a`，`gzip -t` 通过。在线进程最后由 80 秒外层超时终止（退出码 124），但原始记录与候选配置完整。
- 两次离线复算：均为 8 个有效场景、266 条测量、MPFIT 失败 0；两份候选配置 SHA-256 完全相同。在线与离线解的相对位移差约 `0.435 mm`、相对旋转差约 `0.065°`。冻结的是离线解。
- 8 秒预热＋20 秒静止：2772 帧，138.505 Hz；位置 P95/最大 `0.043/0.054 mm`，姿态 P95 `0.085°`，时间戳回退 0。见 `static_20s.json`。
- 独立进程重启，8 秒预热＋10 秒静止：1349 帧，134.702 Hz；位置 P95 `0.023 mm`，姿态 P95 `0.029°`；两轮中心相差 `0.037 mm / 0.025°`。见 `static_restart_10s.json`。
- 4 秒预热＋20 秒缓慢移动：2784 帧，139.121 Hz；>25 ms 间隔 0、时间戳回退 0、位置/角度跳变 0；采集期间基站配置审计通过。见 `dynamic_20s.json`。
- 同一静止位置的 v11 对照：限时内没有 WM0 位姿，日志中 2665 次解算尝试失败 2658 次。该对照不能区分旧几何失效与位置相关的可见性问题，但拒绝继续用 v11 验证新录制。
- 按项目规定先加载 ROS Humble 与工作区环境后，40 秒录制预检 11/11 项通过，含 D405、IMU、Tracker、校验和及空间检查。见 `preflight_full_40s_rosenv.json`。当时 `/dev/shm` 可用 `6.018 GB`、预计需 `5.087 GB`；正式录制前必须再检查一次空间。未加载 ROS 环境的同一次预检仅因找不到 `rclpy` 而失败，并非设备故障。

以上验证的是 Lighthouse Tracker 输出的稳定性，**不是** Tracker→UMI 外参、时间偏移或融合 SLAM 的 10 mm 精度。新录制仍须校验 D405/IMU/Tracker 时间窗重叠、Tracker 分支切换和盲测轨迹精度；不能用 Lighthouse 轨迹反向监督后处理 SLAM。
